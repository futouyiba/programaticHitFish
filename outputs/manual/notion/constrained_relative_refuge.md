> **类型**：受约束相对庇护所（种子）（CONSTRAINED_RELATIVE_REFUGE）｜面：Bake｜状态：CANDIDATE｜名义成员：0

## 特点

live 侧带来的外来假说——普查 7 批 284 条 non-match 0 命中，作负证据台账留册不撤。

## 中文伪脚本（渐进累积语义）

```plain text
（无成员——负证据台账载体）
```

（语义：每步三档＝最适应·全额乘入 / 可接受·×衰减乘入 / 不居留·×0.01 软出局立即返回（非零、仍可参与下游）；无终步合并步；GATE 硬门＝二元 EARLY_RETURN）

## 配置表（参数轴）

| 参数轴 | 取值 / 说明 |
|---|---|
| ledger | 284 non-match（census↔live 对账弹药） |

---

*数据源：template_registry.yaml v10（确定性生成——手册生成器，非手写漂移）*
